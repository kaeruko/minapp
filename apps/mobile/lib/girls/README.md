# MinApp Girls mobile feature

Girls-specific mobile implementation lives in this directory.

Shared mobile code remains under `lib/` and is re-exported through the small adapter files in this directory so Girls-specific files can keep local imports while sharing the same implementation.

Entry point: `../main_girls.dart`
