name: Issue 180 Authoring assets

on:
  push:
    branches:
      - chatgpt/issue-180-authoring-assets
    paths:
      - '.github/workflows/issue180-assets.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: chatgpt/issue-180-authoring-assets
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply asset bridge implementation
        shell: python
        run: |
          from pathlib import Path
          import re

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f'{path}: expected one match, found {count}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1))

          # Native Flutter Authoring bridge.
          path = 'apps/mobile/lib/hosted_authoring_bridge.dart'
          replace_once(path, "import 'dart:convert';\n", "import 'dart:convert';\nimport 'dart:typed_data';\n")
          replace_once(path,
          "final RegExp _authoringRequestIdPattern = RegExp(r'^[A-Za-z0-9_-]{1,64}$');\n",
          """final RegExp _authoringRequestIdPattern = RegExp(r'^[A-Za-z0-9_-]{1,64}$');
          const Map<String, String> _authoringAssetContentTypes = <String, String>{
            '.png': 'image/png',
            '.jpg': 'image/jpeg',
            '.jpeg': 'image/jpeg',
            '.gif': 'image/gif',
            '.webp': 'image/webp',
            '.mp3': 'audio/mpeg',
            '.m4a': 'audio/mp4',
            '.ogg': 'audio/ogg',
            '.wav': 'audio/wav',
          };
          """)
          replace_once(path,
          """abstract interface class HostedAuthoringTransport {
            Future<Map<String, Object?>> loadProject(String authoringToken);

            Future<Map<String, Object?>> saveDocument(
          """,
          """class HostedAuthoringAssetData {
            HostedAuthoringAssetData({required Uint8List bytes, required this.contentType})
                : bytes = Uint8List.fromList(bytes);

            final Uint8List bytes;
            final String contentType;
          }

          abstract interface class HostedAuthoringTransport {
            Future<Map<String, Object?>> loadProject(String authoringToken);

            Future<HostedAuthoringAssetData> getAsset(
              String authoringToken, {
              required String path,
            });

            Future<Map<String, Object?>> saveAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
              required Uint8List bytes,
            });

            Future<Map<String, Object?>> deleteAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
            });

            Future<Map<String, Object?>> saveDocument(
          """)
          replace_once(path,
          """  @override
            Future<Map<String, Object?>> saveDocument(
          """,
          """  @override
            Future<HostedAuthoringAssetData> getAsset(
              String authoringToken, {
              required String path,
            }) async {
              _validateAuthoringToken(authoringToken);
              final String normalizedPath = _validateAuthoringAssetPath(path);
              final String expectedContentType = _authoringAssetContentType(normalizedPath);
              final Uri uri = _baseUri.resolve(_authoringAssetApiPath(authoringToken, normalizedPath));
              final http.Response response = await _client.get(
                uri,
                headers: const <String, String>{'Accept': '*/*'},
              );
              if (response.statusCode < 200 || response.statusCode >= 300) {
                throw _apiException(response.statusCode, _decodeJsonObject(response));
              }
              final String? rawContentType = response.headers['content-type'];
              final String contentType = rawContentType == null
                  ? ''
                  : rawContentType.split(';', 1).first.trim().toLowerCase();
              if (contentType != expectedContentType) {
                throw FormatException(
                  'Authoring asset response Content-Type must be $expectedContentType.',
                );
              }
              return HostedAuthoringAssetData(
                bytes: response.bodyBytes,
                contentType: contentType,
              );
            }

            @override
            Future<Map<String, Object?>> saveAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
              required Uint8List bytes,
            }) async {
              _validateAuthoringToken(authoringToken);
              _validateExpectedRevision(expectedRevision);
              final String normalizedPath = _validateAuthoringAssetPath(path);
              final String contentType = _authoringAssetContentType(normalizedPath);
              final Uri uri = _baseUri.resolve(_authoringAssetApiPath(authoringToken, normalizedPath));
              final http.Response response = await _client.post(
                uri,
                headers: <String, String>{
                  'Accept': 'application/json',
                  'Content-Type': contentType,
                  'x-minapp-expected-revision': '$expectedRevision',
                },
                body: bytes,
              );
              final Map<String, Object?> payload = _decodeJsonObject(response);
              if (response.statusCode < 200 || response.statusCode >= 300) {
                throw _apiException(response.statusCode, payload);
              }
              _validateProjectPayload(payload, includeDocument: false);
              return payload;
            }

            @override
            Future<Map<String, Object?>> deleteAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
            }) async {
              _validateAuthoringToken(authoringToken);
              _validateExpectedRevision(expectedRevision);
              final String normalizedPath = _validateAuthoringAssetPath(path);
              final Uri uri = _baseUri.resolve(_authoringAssetApiPath(authoringToken, normalizedPath));
              final http.Response response = await _client.delete(
                uri,
                headers: <String, String>{
                  'Accept': 'application/json',
                  'x-minapp-expected-revision': '$expectedRevision',
                },
              );
              final Map<String, Object?> payload = _decodeJsonObject(response);
              if (response.statusCode < 200 || response.statusCode >= 300) {
                throw _apiException(response.statusCode, payload);
              }
              _validateProjectPayload(payload, includeDocument: false);
              return payload;
            }

            @override
            Future<Map<String, Object?>> saveDocument(
          """)
          replace_once(path,
          """    this.expectedRevision,
              this.document,
            });

            final String id;
            final String method;
            final int? expectedRevision;
            final Map<String, Object?>? document;
          """,
          """    this.expectedRevision,
              this.document,
              this.assetPath,
              this.assetBytes,
            });

            final String id;
            final String method;
            final int? expectedRevision;
            final Map<String, Object?>? document;
            final String? assetPath;
            final Uint8List? assetBytes;
          """)
          replace_once(path,
          """    if (rawMethod != 'authoring.load' &&
                rawMethod != 'authoring.save' &&
                rawMethod != 'authoring.publish') {
          """,
          """    if (rawMethod != 'authoring.load' &&
                rawMethod != 'authoring.save' &&
                rawMethod != 'authoring.getAsset' &&
                rawMethod != 'authoring.saveAsset' &&
                rawMethod != 'authoring.deleteAsset' &&
                rawMethod != 'authoring.publish') {
          """)
          replace_once(path,
          """    if (rawMethod == 'authoring.load') {
              _requireBridgeFields(
                decoded,
                const <String>{'version', 'id', 'method'},
                id,
              );
              return HostedAuthoringBridgeRequest(id: id, method: rawMethod as String);
            }

            final Set<String> expectedFields = rawMethod == 'authoring.save'
          """,
          """    if (rawMethod == 'authoring.load') {
              _requireBridgeFields(
                decoded,
                const <String>{'version', 'id', 'method'},
                id,
              );
              return HostedAuthoringBridgeRequest(id: id, method: rawMethod as String);
            }

            if (rawMethod == 'authoring.getAsset') {
              _requireBridgeFields(
                decoded,
                const <String>{'version', 'id', 'method', 'path'},
                id,
              );
              return HostedAuthoringBridgeRequest(
                id: id,
                method: rawMethod as String,
                assetPath: _bridgeAssetPath(decoded['path'], id),
              );
            }

            if (rawMethod == 'authoring.saveAsset' || rawMethod == 'authoring.deleteAsset') {
              final Set<String> expectedFields = rawMethod == 'authoring.saveAsset'
                  ? const <String>{'version', 'id', 'method', 'path', 'expectedRevision', 'dataBase64'}
                  : const <String>{'version', 'id', 'method', 'path', 'expectedRevision'};
              _requireBridgeFields(decoded, expectedFields, id);
              final Object? rawRevision = decoded['expectedRevision'];
              if (rawRevision is! int || rawRevision is bool || rawRevision < 1) {
                throw HostedAuthoringBridgeProtocolException(
                  code: 'invalid_expected_revision',
                  message: 'expectedRevision must be a positive integer.',
                  requestId: id,
                );
              }
              Uint8List? assetBytes;
              if (rawMethod == 'authoring.saveAsset') {
                final Object? encoded = decoded['dataBase64'];
                if (encoded is! String || encoded.isEmpty) {
                  throw HostedAuthoringBridgeProtocolException(
                    code: 'invalid_authoring_asset_data',
                    message: 'Authoring asset data must be non-empty base64.',
                    requestId: id,
                  );
                }
                try {
                  assetBytes = base64Decode(encoded);
                } on FormatException {
                  throw HostedAuthoringBridgeProtocolException(
                    code: 'invalid_authoring_asset_data',
                    message: 'Authoring asset data must be valid base64.',
                    requestId: id,
                  );
                }
                if (assetBytes.isEmpty) {
                  throw HostedAuthoringBridgeProtocolException(
                    code: 'invalid_authoring_asset_data',
                    message: 'Authoring asset data must not be empty.',
                    requestId: id,
                  );
                }
              }
              return HostedAuthoringBridgeRequest(
                id: id,
                method: rawMethod as String,
                expectedRevision: rawRevision,
                assetPath: _bridgeAssetPath(decoded['path'], id),
                assetBytes: assetBytes,
              );
            }

            final Set<String> expectedFields = rawMethod == 'authoring.save'
          """)
          replace_once(path,
          """  static Map<String, Object?> success(String id, Object? result) =>
          """,
          """  static String _bridgeAssetPath(Object? value, String id) {
            if (value is! String) {
              throw HostedAuthoringBridgeProtocolException(
                code: 'invalid_authoring_asset_path',
                message: 'Authoring asset path is invalid.',
                requestId: id,
              );
            }
            try {
              return _validateAuthoringAssetPath(value);
            } on ArgumentError {
              throw HostedAuthoringBridgeProtocolException(
                code: 'invalid_authoring_asset_path',
                message: 'Authoring asset path is invalid or unsupported.',
                requestId: id,
              );
            }
          }

            static Map<String, Object?> success(String id, Object? result) =>
          """)
          replace_once(path,
          """  const authoring = Object.freeze({
            load: () => send({ method: 'authoring.load' }),
            save: (data, options) => {
          """,
          """  const ASSET_EXTENSIONS = new Set(['png', 'jpg', 'jpeg', 'gif', 'webp', 'mp3', 'm4a', 'ogg', 'wav']);
            const validateAssetPath = (path) => {
              if (typeof path !== 'string' || path.length === 0 || path.length > 256 ||
                  path.startsWith('/') || path.includes('\\\\') || path.includes('\\0')) {
                throw new MinAppAuthoringError(0, 'invalid_authoring_asset_path', 'Authoring asset path is invalid.');
              }
              const parts = path.split('/');
              if (parts.some((part) => part === '' || part === '.' || part === '..')) {
                throw new MinAppAuthoringError(0, 'invalid_authoring_asset_path', 'Authoring asset path is invalid.');
              }
              const name = parts[parts.length - 1];
              const dot = name.lastIndexOf('.');
              const extension = dot < 0 ? '' : name.slice(dot + 1).toLowerCase();
              if (!ASSET_EXTENSIONS.has(extension)) {
                throw new MinAppAuthoringError(0, 'unsupported_authoring_asset_type', 'Authoring asset type is not supported.');
              }
              return path;
            };
            const encodeAssetBytes = (bytes) => {
              if (!(bytes instanceof Uint8Array) || bytes.length === 0) {
                throw new MinAppAuthoringError(0, 'invalid_authoring_asset_data', 'Authoring asset bytes must be a non-empty Uint8Array.');
              }
              let binary = '';
              for (let offset = 0; offset < bytes.length; offset += 0x8000) {
                binary += String.fromCharCode(...bytes.subarray(offset, Math.min(offset + 0x8000, bytes.length)));
              }
              return btoa(binary);
            };
            const decodeAssetResult = (result) => {
              if (!result || typeof result !== 'object' || Array.isArray(result) ||
                  Object.keys(result).sort().join(',') !== 'contentType,dataBase64' ||
                  typeof result.dataBase64 !== 'string' || result.dataBase64.length === 0 ||
                  typeof result.contentType !== 'string' || result.contentType.length === 0) {
                throw new MinAppAuthoringError(0, 'invalid_bridge_response', 'Authoring asset response is invalid.');
              }
              const binary = atob(result.dataBase64);
              const bytes = new Uint8Array(binary.length);
              for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index);
              return Object.freeze({ bytes, contentType: result.contentType });
            };

            const authoring = Object.freeze({
              load: () => send({ method: 'authoring.load' }),
              getAsset: (path) => {
                try {
                  return send({ method: 'authoring.getAsset', path: validateAssetPath(path) }).then(decodeAssetResult);
                } catch (error) {
                  return Promise.reject(error);
                }
              },
              saveAsset: (path, bytes, options) => {
                let expectedRevision;
                let normalizedPath;
                let dataBase64;
                try {
                  normalizedPath = validateAssetPath(path);
                  dataBase64 = encodeAssetBytes(bytes);
                  expectedRevision = validateExpectedRevision(options);
                } catch (error) {
                  return Promise.reject(error);
                }
                return send({ method: 'authoring.saveAsset', path: normalizedPath, expectedRevision, dataBase64 });
              },
              deleteAsset: (path, options) => {
                let expectedRevision;
                let normalizedPath;
                try {
                  normalizedPath = validateAssetPath(path);
                  expectedRevision = validateExpectedRevision(options);
                } catch (error) {
                  return Promise.reject(error);
                }
                return send({ method: 'authoring.deleteAsset', path: normalizedPath, expectedRevision });
              },
              save: (data, options) => {
          """)
          replace_once(path,
          """      if (request.method == 'authoring.load') {
                result = await _transport.loadProject(_authoringToken);
              } else if (request.method == 'authoring.save') {
          """,
          """      if (request.method == 'authoring.load') {
                result = await _transport.loadProject(_authoringToken);
              } else if (request.method == 'authoring.getAsset') {
                final String? assetPath = request.assetPath;
                if (assetPath == null) {
                  throw StateError('Validated Authoring getAsset request lost path.');
                }
                final HostedAuthoringAssetData asset = await _transport.getAsset(
                  _authoringToken,
                  path: assetPath,
                );
                result = <String, Object?>{
                  'dataBase64': base64Encode(asset.bytes),
                  'contentType': asset.contentType,
                };
              } else if (request.method == 'authoring.saveAsset') {
                final int? expectedRevision = request.expectedRevision;
                final String? assetPath = request.assetPath;
                final Uint8List? assetBytes = request.assetBytes;
                if (expectedRevision == null || assetPath == null || assetBytes == null) {
                  throw StateError('Validated Authoring saveAsset request lost required fields.');
                }
                result = await _transport.saveAsset(
                  _authoringToken,
                  expectedRevision: expectedRevision,
                  path: assetPath,
                  bytes: assetBytes,
                );
              } else if (request.method == 'authoring.deleteAsset') {
                final int? expectedRevision = request.expectedRevision;
                final String? assetPath = request.assetPath;
                if (expectedRevision == null || assetPath == null) {
                  throw StateError('Validated Authoring deleteAsset request lost required fields.');
                }
                result = await _transport.deleteAsset(
                  _authoringToken,
                  expectedRevision: expectedRevision,
                  path: assetPath,
                );
              } else if (request.method == 'authoring.save') {
          """)
          replace_once(path,
          """void _validateExpectedRevision(int value) {
          """,
          """String _validateAuthoringAssetPath(String path) {
            if (path.isEmpty || path.length > 256 || path.contains('\\\\') || path.contains('\\u0000') || path.startsWith('/')) {
              throw ArgumentError.value(path, 'path', 'is not a valid Authoring asset path');
            }
            final List<String> parts = path.split('/');
            if (parts.any((String part) => part.isEmpty || part == '.' || part == '..')) {
              throw ArgumentError.value(path, 'path', 'is not a valid Authoring asset path');
            }
            _authoringAssetContentType(path);
            return parts.join('/');
          }

          String _authoringAssetContentType(String path) {
            final String name = path.split('/').last;
            final int dot = name.lastIndexOf('.');
            final String suffix = dot < 0 ? '' : name.substring(dot).toLowerCase();
            final String? contentType = _authoringAssetContentTypes[suffix];
            if (contentType == null) {
              throw ArgumentError.value(path, 'path', 'has an unsupported Authoring asset type');
            }
            return contentType;
          }

          String _authoringAssetApiPath(String token, String path) {
            final String encodedPath = path.split('/').map(Uri.encodeComponent).join('/');
            return '/hosted/authoring/session/$token/assets/$encodedPath';
          }

          void _validateExpectedRevision(int value) {
          """)

          # Native tests.
          path = 'apps/mobile/test/hosted_authoring_bridge_test.dart'
          replace_once(path, "import 'dart:convert';\n", "import 'dart:convert';\nimport 'dart:typed_data';\n")
          replace_once(path,
          """  @override
            Future<Map<String, Object?>> saveDocument(
          """,
          """  @override
            Future<HostedAuthoringAssetData> getAsset(
              String authoringToken, {
              required String path,
            }) async {
              calls.add('getAsset:$authoringToken:$path');
              return HostedAuthoringAssetData(
                bytes: Uint8List.fromList(<int>[1, 2, 3]),
                contentType: 'image/png',
              );
            }

            @override
            Future<Map<String, Object?>> saveAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
              required Uint8List bytes,
            }) async {
              calls.add('saveAsset:$authoringToken:$expectedRevision:$path:${base64Encode(bytes)}');
              return _project(revision: expectedRevision + 1);
            }

            @override
            Future<Map<String, Object?>> deleteAsset(
              String authoringToken, {
              required int expectedRevision,
              required String path,
            }) async {
              calls.add('deleteAsset:$authoringToken:$expectedRevision:$path');
              return _project(revision: expectedRevision + 1);
            }

            @override
            Future<Map<String, Object?>> saveDocument(
          """)
          insert_before = "  test('publish response rejects invalid normal app identity', () async {\n"
          asset_test = r'''  test('asset API uses the scoped token with exact MIME and revision headers', () async {
            final List<http.Request> requests = <http.Request>[];
            final MockClient client = MockClient((http.Request request) async {
              requests.add(request);
              expect(request.headers['authorization'], isNull);
              if (request.method == 'GET') {
                return http.Response.bytes(
                  <int>[1, 2, 3],
                  200,
                  headers: const <String, String>{'content-type': 'image/png'},
                );
              }
              if (request.method == 'POST') {
                expect(request.headers['content-type'], 'image/png');
                expect(request.headers['x-minapp-expected-revision'], '7');
                expect(request.bodyBytes, <int>[4, 5, 6]);
                return _json(200, _project(revision: 8));
              }
              if (request.method == 'DELETE') {
                expect(request.headers['x-minapp-expected-revision'], '8');
                return _json(200, _project(revision: 9));
              }
              fail('Unexpected asset request ${request.method}');
            });
            final HostedAuthoringApiClient api = HostedAuthoringApiClient(
              baseUri: Uri.parse('https://hosted.example.test'),
              client: client,
            );

            final HostedAuthoringAssetData asset = await api.getAsset(
              _token,
              path: 'images/face.png',
            );
            final Map<String, Object?> saved = await api.saveAsset(
              _token,
              expectedRevision: 7,
              path: 'images/face.png',
              bytes: Uint8List.fromList(<int>[4, 5, 6]),
            );
            final Map<String, Object?> deleted = await api.deleteAsset(
              _token,
              expectedRevision: 8,
              path: 'images/face.png',
            );

            expect(asset.bytes, <int>[1, 2, 3]);
            expect(asset.contentType, 'image/png');
            expect(saved['draft_revision'], 8);
            expect(deleted['draft_revision'], 9);
            expect(
              requests.map((http.Request request) => request.url.path).toList(),
              <String>[
                '/hosted/authoring/session/$_token/assets/images/face.png',
                '/hosted/authoring/session/$_token/assets/images/face.png',
                '/hosted/authoring/session/$_token/assets/images/face.png',
              ],
            );
          });

          '''
          replace_once(path, insert_before, asset_test + insert_before)
          replace_once(path,
          """    final Map<String, Object?> save = await bridge.handleMessage(
          """,
          """    final Map<String, Object?> getAsset = await bridge.handleMessage(
                jsonEncode(<String, Object?>{
                  'version': 1,
                  'id': 'asset-get',
                  'method': 'authoring.getAsset',
                  'path': 'images/face.png',
                }),
              );
              final Map<String, Object?> saveAsset = await bridge.handleMessage(
                jsonEncode(<String, Object?>{
                  'version': 1,
                  'id': 'asset-save',
                  'method': 'authoring.saveAsset',
                  'path': 'images/face.png',
                  'expectedRevision': 7,
                  'dataBase64': base64Encode(<int>[4, 5, 6]),
                }),
              );
              final Map<String, Object?> deleteAsset = await bridge.handleMessage(
                jsonEncode(<String, Object?>{
                  'version': 1,
                  'id': 'asset-delete',
                  'method': 'authoring.deleteAsset',
                  'path': 'images/face.png',
                  'expectedRevision': 8,
                }),
              );
              final Map<String, Object?> save = await bridge.handleMessage(
          """)
          replace_once(path,
          """    expect(load['ok'], isTrue);
            expect(save['ok'], isTrue);
            expect(publish['ok'], isTrue);
            expect(
              transport.calls,
              <String>[
                'load:$_token',
                'save:$_token:7:1',
                'publish:$_token:8',
              ],
            );
          """,
          """    expect(load['ok'], isTrue);
            expect(getAsset['ok'], isTrue);
            expect(saveAsset['ok'], isTrue);
            expect(deleteAsset['ok'], isTrue);
            expect(save['ok'], isTrue);
            expect(publish['ok'], isTrue);
            expect(
              (getAsset['result']! as Map<String, Object?>)['dataBase64'],
              base64Encode(<int>[1, 2, 3]),
            );
            expect(
              transport.calls,
              <String>[
                'load:$_token',
                'getAsset:$_token:images/face.png',
                'saveAsset:$_token:7:images/face.png:${base64Encode(<int>[4, 5, 6])}',
                'deleteAsset:$_token:8:images/face.png',
                'save:$_token:7:1',
                'publish:$_token:8',
              ],
            );
          """)
          replace_once(path,
          """    expect(script, contains('authoring.save'));
            expect(script, contains('authoring.publish'));
          """,
          """    expect(script, contains('authoring.save'));
            expect(script, contains('authoring.getAsset'));
            expect(script, contains('authoring.saveAsset'));
            expect(script, contains('authoring.deleteAsset'));
            expect(script, contains('Uint8Array'));
            expect(script, contains('authoring.publish'));
          """)

          # Web parent Host Adapter.
          path = 'apps/web/authoring_host_adapter.js'
          replace_once(path,
          """  const AUTHORING_METHODS = new Set([
              "authoring.load",
              "authoring.save",
              "authoring.preview",
              "authoring.publish",
            ]);
          """,
          """  const AUTHORING_METHODS = new Set([
              "authoring.load",
              "authoring.save",
              "authoring.getAsset",
              "authoring.saveAsset",
              "authoring.deleteAsset",
              "authoring.preview",
              "authoring.publish",
            ]);
            const AUTHORING_ASSET_CONTENT_TYPES = Object.freeze({
              png: "image/png",
              jpg: "image/jpeg",
              jpeg: "image/jpeg",
              gif: "image/gif",
              webp: "image/webp",
              mp3: "audio/mpeg",
              m4a: "audio/mp4",
              ogg: "audio/ogg",
              wav: "audio/wav",
            });
          """)
          replace_once(path,
          """  function validateAccessToken(value) {
          """,
          """  function validateAuthoringAssetPath(value) {
            if (typeof value !== "string" || value.length === 0 || value.length > 256 ||
                value.startsWith("/") || value.includes("\\\\") || value.includes("\\0")) {
              throw new Error("Authoring asset path is invalid.");
            }
            const parts = value.split("/");
            if (parts.some((part) => part === "" || part === "." || part === "..")) {
              throw new Error("Authoring asset path is invalid.");
            }
            const name = parts[parts.length - 1];
            const dot = name.lastIndexOf(".");
            const extension = dot < 0 ? "" : name.slice(dot + 1).toLowerCase();
            if (!Object.prototype.hasOwnProperty.call(AUTHORING_ASSET_CONTENT_TYPES, extension)) {
              throw new Error("Authoring asset type is not supported.");
            }
            return value;
          }

            function authoringAssetContentType(path) {
              const normalized = validateAuthoringAssetPath(path);
              const name = normalized.split("/").at(-1);
              return AUTHORING_ASSET_CONTENT_TYPES[name.slice(name.lastIndexOf(".") + 1).toLowerCase()];
            }

            function authoringAssetApiPath(token, path) {
              const encoded = validateAuthoringAssetPath(path).split("/").map(encodeURIComponent).join("/");
              return `/hosted/authoring/session/${token}/assets/${encoded}`;
            }

            function decodeBase64Bytes(value) {
              if (typeof value !== "string" || value.length === 0) throw new Error("Authoring asset data must be non-empty base64.");
              let binary;
              try { binary = atob(value); }
              catch (error) { throw new Error("Authoring asset data must be valid base64.", { cause: error }); }
              const bytes = new Uint8Array(binary.length);
              for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index);
              if (bytes.length === 0) throw new Error("Authoring asset data must not be empty.");
              return bytes;
            }

            function encodeBase64Bytes(bytes) {
              let binary = "";
              for (let offset = 0; offset < bytes.length; offset += 0x8000) {
                binary += String.fromCharCode(...bytes.subarray(offset, Math.min(offset + 0x8000, bytes.length)));
              }
              return btoa(binary);
            }

            async function assetRequest({ apiOrigin, fetchImpl, authoringToken, method, path, expectedRevision = null, dataBase64 = null }) {
              const origin = validateApiOrigin(apiOrigin);
              validateFetch(fetchImpl);
              validateToken(authoringToken, "authoringToken");
              const normalizedPath = validateAuthoringAssetPath(path);
              if (!["GET", "POST", "DELETE"].includes(method)) throw new TypeError("Unsupported asset request method.");
              if ((method === "POST" || method === "DELETE") && (!Number.isInteger(expectedRevision) || expectedRevision < 1)) {
                throw new Error("expectedRevision must be a positive integer.");
              }
              const url = new URL(authoringAssetApiPath(authoringToken, normalizedPath), `${origin}/`);
              if (url.origin !== origin) throw new Error("Authoring asset path escaped configured origin.");
              const headers = { Accept: method === "GET" ? "*/*" : "application/json" };
              const options = { method, headers, cache: "no-store", credentials: "omit" };
              if (method === "POST" || method === "DELETE") headers["x-minapp-expected-revision"] = String(expectedRevision);
              if (method === "POST") {
                headers["Content-Type"] = authoringAssetContentType(normalizedPath);
                options.body = decodeBase64Bytes(dataBase64);
              }
              let response;
              try { response = await fetchImpl(url.toString(), options); }
              catch (error) { throw new HostedWebApiError(0, "host_adapter_request_failed", String(error)); }
              if (method === "GET" && response.ok) {
                const rawContentType = response.headers.get("content-type");
                const contentType = rawContentType === null ? "" : rawContentType.split(";", 1)[0].trim().toLowerCase();
                const expectedContentType = authoringAssetContentType(normalizedPath);
                if (contentType !== expectedContentType) {
                  throw new HostedWebApiError(response.status, "invalid_api_response", `Authoring asset Content-Type must be ${expectedContentType}.`);
                }
                const bytes = new Uint8Array(await response.arrayBuffer());
                return Object.freeze({ dataBase64: encodeBase64Bytes(bytes), contentType });
              }
              return await decodeJsonResponse(response, `Authoring asset ${method}`);
            }

            function validateAccessToken(value) {
          """)
          replace_once(path,
          "const requiredOperations = [\"load\", \"save_document\", \"preview_request\", \"publish_request\"];",
          "const requiredOperations = [\"load\", \"save_document\", \"get_asset\", \"save_asset\", \"delete_asset\", \"preview_request\", \"publish_request\"];")
          replace_once(path,
          """    if (data.method === "authoring.load") {
              try {
                requireExactFields(data, common, "Authoring load request");
              } catch (error) {
                throw protocolError("invalid_bridge_request", "Authoring load request fields are invalid.", id);
              }
              return Object.freeze({ id, method: data.method });
            }

            const expected = data.method === "authoring.save"
          """,
          """    if (data.method === "authoring.load") {
              try {
                requireExactFields(data, common, "Authoring load request");
              } catch (error) {
                throw protocolError("invalid_bridge_request", "Authoring load request fields are invalid.", id);
              }
              return Object.freeze({ id, method: data.method });
            }

            if (data.method === "authoring.getAsset") {
              try {
                requireExactFields(data, [...common, "path"], "Authoring getAsset request");
                return Object.freeze({ id, method: data.method, path: validateAuthoringAssetPath(data.path) });
              } catch (error) {
                throw protocolError("invalid_authoring_asset_path", "Authoring asset path is invalid or unsupported.", id);
              }
            }

            if (data.method === "authoring.saveAsset" || data.method === "authoring.deleteAsset") {
              const expectedAssetFields = data.method === "authoring.saveAsset"
                ? [...common, "path", "expectedRevision", "dataBase64"]
                : [...common, "path", "expectedRevision"];
              try {
                requireExactFields(data, expectedAssetFields, "Authoring asset request");
              } catch (error) {
                throw protocolError("invalid_bridge_request", "Authoring asset request fields are invalid.", id);
              }
              if (!Number.isInteger(data.expectedRevision) || data.expectedRevision < 1) {
                throw protocolError("invalid_expected_revision", "expectedRevision must be a positive integer.", id);
              }
              let normalizedPath;
              try { normalizedPath = validateAuthoringAssetPath(data.path); }
              catch (error) { throw protocolError("invalid_authoring_asset_path", "Authoring asset path is invalid or unsupported.", id); }
              if (data.method === "authoring.saveAsset") {
                try { decodeBase64Bytes(data.dataBase64); }
                catch (error) { throw protocolError("invalid_authoring_asset_data", error.message, id); }
              }
              return Object.freeze({
                id,
                method: data.method,
                path: normalizedPath,
                expectedRevision: data.expectedRevision,
                ...(data.method === "authoring.saveAsset" ? { dataBase64: data.dataBase64 } : {}),
              });
            }

            const expected = data.method === "authoring.save"
          """)
          replace_once(path,
          """      if (request.method === "authoring.save") {
                return await jsonRequest({
          """,
          """      if (request.method === "authoring.getAsset") {
                return await assetRequest({
                  apiOrigin: this.apiOrigin,
                  fetchImpl: this.fetchImpl,
                  authoringToken: this.launch.authoringToken,
                  method: "GET",
                  path: request.path,
                });
              }
              if (request.method === "authoring.saveAsset") {
                return await assetRequest({
                  apiOrigin: this.apiOrigin,
                  fetchImpl: this.fetchImpl,
                  authoringToken: this.launch.authoringToken,
                  method: "POST",
                  path: request.path,
                  expectedRevision: request.expectedRevision,
                  dataBase64: request.dataBase64,
                });
              }
              if (request.method === "authoring.deleteAsset") {
                return await assetRequest({
                  apiOrigin: this.apiOrigin,
                  fetchImpl: this.fetchImpl,
                  authoringToken: this.launch.authoringToken,
                  method: "DELETE",
                  path: request.path,
                  expectedRevision: request.expectedRevision,
                });
              }
              if (request.method === "authoring.save") {
                return await jsonRequest({
          """)

          # Web child bootstrap injected by backend.
          path = 'backend/src/hosted_authoring_web_bridge.py'
          replace_once(path,
          """  const STATE_KEY = /^[a-z][a-z0-9_.-]{{0,63}}$/;
          """,
          """  const STATE_KEY = /^[a-z][a-z0-9_.-]{{0,63}}$/;
            const ASSET_EXTENSIONS = new Set(['png', 'jpg', 'jpeg', 'gif', 'webp', 'mp3', 'm4a', 'ogg', 'wav']);
          """)
          replace_once(path,
          """  const validateRevision = (options) => {{
          """,
          """  const validateAssetPath = (path) => {{
              if (typeof path !== 'string' || path.length === 0 || path.length > 256 ||
                  path.startsWith('/') || path.includes('\\\\') || path.includes('\\0')) {{
                throw new MinAppError(0, 'invalid_authoring_asset_path', 'Authoring asset path is invalid.');
              }}
              const parts = path.split('/');
              if (parts.some((part) => part === '' || part === '.' || part === '..')) {{
                throw new MinAppError(0, 'invalid_authoring_asset_path', 'Authoring asset path is invalid.');
              }}
              const name = parts[parts.length - 1];
              const dot = name.lastIndexOf('.');
              const extension = dot < 0 ? '' : name.slice(dot + 1).toLowerCase();
              if (!ASSET_EXTENSIONS.has(extension)) {{
                throw new MinAppError(0, 'unsupported_authoring_asset_type', 'Authoring asset type is not supported.');
              }}
              return path;
            }};
            const encodeAssetBytes = (bytes) => {{
              if (!(bytes instanceof Uint8Array) || bytes.length === 0) {{
                throw new MinAppError(0, 'invalid_authoring_asset_data', 'Authoring asset bytes must be a non-empty Uint8Array.');
              }}
              let binary = '';
              for (let offset = 0; offset < bytes.length; offset += 0x8000) {{
                binary += String.fromCharCode(...bytes.subarray(offset, Math.min(offset + 0x8000, bytes.length)));
              }}
              return btoa(binary);
            }};
            const decodeAssetResult = (result) => {{
              if (!isPlainObject(result) ||
                  Object.keys(result).sort().join(',') !== 'contentType,dataBase64' ||
                  typeof result.dataBase64 !== 'string' || result.dataBase64.length === 0 ||
                  typeof result.contentType !== 'string' || result.contentType.length === 0) {{
                throw new MinAppError(0, 'invalid_bridge_response', 'Authoring asset response is invalid.');
              }}
              const binary = atob(result.dataBase64);
              const bytes = new Uint8Array(binary.length);
              for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index);
              return Object.freeze({{ bytes, contentType: result.contentType }});
            }};
            const validateRevision = (options) => {{
          """)
          replace_once(path,
          """  const authoring = Object.freeze({{
              load: () => send({{ method: 'authoring.load' }}),
              save: (data, options) => {{
          """,
          """  const authoring = Object.freeze({{
              load: () => send({{ method: 'authoring.load' }}),
              getAsset: (path) => {{
                try {{ return send({{ method: 'authoring.getAsset', path: validateAssetPath(path) }}).then(decodeAssetResult); }}
                catch (error) {{ return Promise.reject(error); }}
              }},
              saveAsset: (path, bytes, options) => {{
                let expectedRevision;
                let normalizedPath;
                let dataBase64;
                try {{
                  normalizedPath = validateAssetPath(path);
                  dataBase64 = encodeAssetBytes(bytes);
                  expectedRevision = validateRevision(options);
                }} catch (error) {{ return Promise.reject(error); }}
                return send({{ method: 'authoring.saveAsset', path: normalizedPath, expectedRevision, dataBase64 }});
              }},
              deleteAsset: (path, options) => {{
                let expectedRevision;
                let normalizedPath;
                try {{
                  normalizedPath = validateAssetPath(path);
                  expectedRevision = validateRevision(options);
                }} catch (error) {{ return Promise.reject(error); }}
                return send({{ method: 'authoring.deleteAsset', path: normalizedPath, expectedRevision }});
              }},
              save: (data, options) => {{
          """)

          # Web adapter tests.
          path = 'apps/web/authoring_host_adapter.test.js'
          replace_once(path,
          'allowed_operations: ["load", "save_document", "preview_request", "publish_request"],',
          'allowed_operations: ["load", "save_document", "get_asset", "save_asset", "delete_asset", "preview_request", "publish_request"],')
          replace_once(path,
          'allowedOperations: Object.freeze(["load", "save_document", "preview_request", "publish_request"]),',
          'allowedOperations: Object.freeze(["load", "save_document", "get_asset", "save_asset", "delete_asset", "preview_request", "publish_request"]),')
          replace_once(path,
          """function emptyResponse(status = 204) {
          """,
          """function binaryResponse(status, bytes, contentType) {
            const data = Uint8Array.from(bytes);
            return {
              status,
              ok: status >= 200 && status < 300,
              headers: { get(name) { return name.toLowerCase() === "content-type" ? contentType : null; } },
              async arrayBuffer() { return data.buffer.slice(data.byteOffset, data.byteOffset + data.byteLength); },
              async text() { return ""; },
              async json() { throw new Error("binary response"); },
            };
          }

            function emptyResponse(status = 204) {
          """)
          insert_before = 'test("Backend Authoring error status/code/message are preserved", async () => {\n'
          asset_test = r'''test("Asset bridge keeps scope in trusted parent and preserves bytes/MIME/revisions", async () => {
            const { frame, messages } = fakeFrame();
            const target = fakeEventTarget();
            const calls = [];
            const adapter = new WebAuthoringHostAdapter({
              frame,
              apiOrigin: API_ORIGIN,
              launch: launchGrant(),
              previewHandler: async () => null,
              fetchImpl: async (url, options) => {
                calls.push({ url, options });
                if (options.method === "GET") return binaryResponse(200, [1, 2, 3], "image/png");
                if (options.method === "POST") {
                  assert.equal(options.headers["Content-Type"], "image/png");
                  assert.equal(options.headers["x-minapp-expected-revision"], "7");
                  assert.deepEqual(Array.from(options.body), [4, 5, 6]);
                  return jsonResponse(200, { content_id: CONTENT_ID, group_id: "2".repeat(32), content_format: "example/quiz@1", status: "draft", draft_revision: 8, assets: [], created_at: "2026-09-07T00:00:00Z", updated_at: "2026-09-07T00:00:01Z" });
                }
                assert.equal(options.method, "DELETE");
                assert.equal(options.headers["x-minapp-expected-revision"], "8");
                return jsonResponse(200, { content_id: CONTENT_ID, group_id: "2".repeat(32), content_format: "example/quiz@1", status: "draft", draft_revision: 9, assets: [], created_at: "2026-09-07T00:00:00Z", updated_at: "2026-09-07T00:00:02Z" });
              },
              eventTarget: target,
            });

            await adapter.handleMessageEvent({ source: frame.contentWindow, origin: "null", data: request("authoring.getAsset", { path: "images/face.png" }, "asset-get") });
            await adapter.handleMessageEvent({ source: frame.contentWindow, origin: "null", data: request("authoring.saveAsset", { path: "images/face.png", expectedRevision: 7, dataBase64: "BAUG" }, "asset-save") });
            await adapter.handleMessageEvent({ source: frame.contentWindow, origin: "null", data: request("authoring.deleteAsset", { path: "images/face.png", expectedRevision: 8 }, "asset-delete") });

            assert.equal(calls.length, 3);
            for (const call of calls) {
              assert.equal(call.url, `${API_ORIGIN}/hosted/authoring/session/${AUTHORING_TOKEN}/assets/images/face.png`);
              assert.equal(call.options.headers.Authorization, undefined);
            }
            assert.deepEqual(messages[0].message.result, { dataBase64: "AQID", contentType: "image/png" });
            assert.equal(messages[1].message.result.draft_revision, 8);
            assert.equal(messages[2].message.result.draft_revision, 9);
          });

          test("Asset bridge rejects unsafe path before any fetch", async () => {
            assert.throws(
              () => decodeBridgeRequest(request("authoring.getAsset", { path: "../face.png" }), NONCE),
              (error) => error instanceof WebAuthoringBridgeProtocolError && error.code === "invalid_authoring_asset_path",
            );
          });

          '''
          replace_once(path, insert_before, asset_test + insert_before)

          # Backend web-bootstrap tests.
          path = 'backend/tests/test_hosted_authoring_web_bridge.py'
          replace_once(path,
          '        self.assertIn("authoring.preview", source)\n',
          '        self.assertIn("authoring.preview", source)\n        self.assertIn("authoring.getAsset", source)\n        self.assertIn("authoring.saveAsset", source)\n        self.assertIn("authoring.deleteAsset", source)\n        self.assertIn("Uint8Array", source)\n')
          replace_once(path,
          '            "allowed_operations": ["load", "save_document", "preview_request", "publish_request"],',
          '            "allowed_operations": ["load", "save_document", "get_asset", "save_asset", "delete_asset", "preview_request", "publish_request"],')

          # Keep the PR diff free of the one-shot migration workflow.
          Path('.github/workflows/issue180-assets.yml').unlink()
      - name: Test backend Web bridge
        run: python -m unittest backend.tests.test_hosted_authoring_web_bridge -v
      - name: Test Web Host Adapter
        run: node --test apps/web/authoring_host_adapter.test.js
      - name: Check modified syntax
        run: |
          python -m py_compile backend/src/hosted_authoring_web_bridge.py backend/tests/test_hosted_authoring_web_bridge.py
          node --check apps/web/authoring_host_adapter.js
          node --check apps/web/authoring_host_adapter.test.js
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/mobile/lib/hosted_authoring_bridge.dart apps/mobile/test/hosted_authoring_bridge_test.dart apps/web/authoring_host_adapter.js apps/web/authoring_host_adapter.test.js backend/src/hosted_authoring_web_bridge.py backend/tests/test_hosted_authoring_web_bridge.py .github/workflows/issue180-assets.yml
          git commit -m "#180 expose scoped Authoring asset bridge"
          git push origin HEAD:chatgpt/issue-180-authoring-assets
