[project]
name = "minapp-backend"
version = "0.1.0"
description = "みんアプ serverless API"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
